# todoapp

The app will have two DB : 
    - PostgresQL : contains the login-info as well as sync the tasks 
    - SQLite : run in the front end allow the app to be run offline 

The goal : Implement synchronisation. (sync between all devices of the same account)
